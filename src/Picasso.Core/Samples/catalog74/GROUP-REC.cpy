      *> GROUP-REC — one record per group.
      *> Mirrors Splitty's (:Group) node (title only — no currency/icon,
      *> out of scope for this demo).
       01  GROUP-REC.
           05  GROUP-ID            PIC 9(6).
           05  GROUP-NAME          PIC X(30).
